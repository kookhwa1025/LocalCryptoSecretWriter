# 로컬 암호기반 비밀글 작성 프로그램 (Local Crypto Secret Writer)

## 1. 개요
HTML, CSS, JavaScript만을 사용하여 **백엔드 없이** 작동하는 암호화 기반 비밀글 작성 웹앱.  
RSA를 사용해 AES 키를 전달하고, AES로 실제 내용을 암호화한다.  
암호문은 브라우저의 **LocalStorage**에 저장되어, 새로고침 후에도 데이터가 유지된다.  
사용자는 프로그램 실행 시 **비밀글 작성** 또는 **비밀글 열람** 중 하나를 선택할 수 있다.

---

## 2. 주요 기능 요약

| 구분 | 기능 | 설명 |
|------|------|------|
| 1 | **홈 화면** | 프로그램 시작 시 “비밀글 쓰기 / 비밀글 찾기” 두 버튼 표시 |
| 2 | **비밀글 작성 화면** | 글 내용과 비밀번호 입력 → RSA로 AES키 암호화 후 저장 |
| 3 | **비밀글 저장** | LocalStorage에 `{ id, title, ciphertext, encryptedKey, iv }` 형식으로 저장 |
| 4 | **비밀글 열람 화면** | 저장된 글 목록 표시 → 비밀번호 입력 시 복호화 후 내용 출력 |
| 5 | **암호화 과정 시각화** | RSA 키 생성 → AES 키 생성 → 암호화 → 저장 단계를 애니메이션으로 표시 |
| 6 | **복호화 과정 시각화** | 입력된 비밀번호 → RSA 복호화 → AES 복호화 → 평문 표시 단계 시각화 |
| 7 | **UI 테마** | 다크 차콜 배경, 자물쇠 아이콘, 보안 느낌의 인터랙션(글로우 효과 등) 적용 |

---

## 3. 기술 스택

| 구성 요소 | 기술 |
|------------|------|
| 구조 | HTML5 |
| 스타일 | CSS3 (Flexbox/Grid, 다크 테마, Transition 효과) |
| 동작 | JavaScript (RSA/AES 구현 및 LocalStorage 사용) |
| 암호 알고리즘 | WebCrypto API 기반 RSA-OAEP + AES-GCM |
| 저장소 | LocalStorage (비밀번호 및 암호문 저장용) |

---

## 4. 동작 구조 (시퀀스 요약)

### (1) 비밀글 작성 과정
1. 사용자가 “비밀글 쓰기” 클릭  
2. 평문 입력 + 비밀번호 입력  
3. 브라우저에서 RSA 키쌍 생성 (1회 세션당 또는 캐시 유지 가능)  
4. AES 256비트 키 생성  
5. AES 키를 RSA 공개키로 암호화  
6. 평문을 AES로 암호화 (AES-GCM 사용)  
7. LocalStorage에 암호문, 암호화된 AES키, IV, 제목 등 저장  
8. “암호화 완료” 애니메이션 표시

### (2) 비밀글 열람 과정
1. 사용자가 “비밀글 찾기” 클릭  
2. 저장된 글 목록 표시  
3. 선택 시 비밀번호 입력 요청  
4. RSA 개인키 복호화 시뮬레이션 + AES 복호화 과정 시각화  
5. 평문 복원 후 출력

---

## 5. 데이터 구조 (LocalStorage 저장 예시)

```json
{
  "secrets": [
    {
      "id": "msg_001",
      "title": "나의 첫 번째 비밀글",
      "ciphertext": "base64encodedAEScipher",
      "encryptedKey": "base64encodedRSAcipher",
      "iv": "base64encodedIV",
      "createdAt": "2025-11-02T12:00:00Z"
    }
  ]
}
```

---

## 6. 화면 설계 요약

| 화면명 | 주요 요소 | 설명 |
|---------|------------|------|
| **홈화면** | 제목 “🔒 비밀글 암호 프로그램”, “비밀글 쓰기”, “비밀글 찾기” 버튼 | 중앙 정렬, 차콜 배경, 글로우 효과 버튼 |
| **비밀글 작성 화면** | 텍스트 입력, 비밀번호 입력, “암호화 및 저장” 버튼 | 입력 시 배경에 암호화 애니메이션 표시 |
| **암호화 과정 시각화** | RSA 키 → AES 키 → 암호화 단계 진행바 | progress bar + 작은 정보 텍스트 |
| **비밀글 목록 화면** | 저장된 글 제목 리스트, 클릭 시 비밀번호 입력창 | hover 시 블루 네온 테두리 효과 |
| **비밀글 복호화 화면** | 복호화 단계 애니메이션 + 복원된 평문 표시 | “닫기” 버튼으로 돌아가기 가능 |

---

## 7. 암호화 알고리즘 설계 세부

- **RSA-OAEP (2048비트)**  
  - 공개키로 AES 키 암호화  
  - 개인키는 브라우저 메모리에만 유지 (저장하지 않음)

- **AES-GCM (256비트)**  
  - 평문 암호화  
  - IV는 12바이트 랜덤 생성  
  - 인증 태그 자동 포함  

- **비밀번호 사용 방식**  
  - 사용자가 입력한 비밀번호는 RSA 키 생성 시 entropy seed로만 사용  
  - 저장되지 않음  
  - 열람 시 같은 비밀번호 입력으로 동일한 RSA 개인키 재생성  

---

## 8. 시각 디자인

- **메인 컬러:** `#1c1c1e` (차콜)  
- **포인트 컬러:** `#00ffff` (사이안 글로우 효과)  
- **폰트:** “Roboto Mono”, sans-serif  
- **아이콘:** SVG 자물쇠, 열쇠, 암호화 기호  
- **애니메이션:**  
  - 버튼 hover 시 네온 라인 확장  
  - 암호화 과정에서 데이터 흐름선 시각화  

---

## 9. 추가 요구사항

- LocalStorage의 비밀글 개수는 제한 없음  
- 새로고침 후에도 데이터 유지  
- 각 비밀글은 독립적으로 복호화 가능  
- 반응형 UI (모바일/데스크톱 대응)  
- 외부 네트워크 통신 없음 (100% 로컬 실행)

---

## 10. 향후 확장 포인트 (선택사항)

- IndexedDB로 확장 시 대용량 암호문 저장 가능  
- 비밀번호 힌트 기능 추가  
- RSA 키를 export/import하여 타기기 간 전송 가능  
